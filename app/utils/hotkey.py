"""
全局热键监听模块

需要写的内容:
1. DEFAULT_HOTKEYS 字典 (默认热键配置)
   - capture_fullscreen: Ctrl+Shift+G
   - capture_region: Ctrl+Shift+R
   - open_main_window: Ctrl+Shift+M

2. KEY_MAP 字典 (键名到 Key 枚举的映射)

3. HotkeyManager 类 (单例)
   - 监听器管理 (pynput.keyboard.Listener)
   - 热键注册/注销
   - 按键状态追踪
   - 热键匹配逻辑
   - 启动/停止监听

4. parse_hotkey_string 函数 (解析热键字符串)

5. 全局实例 hotkey_manager

依赖: pynput
"""

from typing import Callable, Dict, Set, Optional

# TODO: 导入 pynput.keyboard

# TODO: 定义 DEFAULT_HOTKEYS

# TODO: 定义 KEY_MAP

# TODO: 定义 HotkeyManager 类

# TODO: 定义 parse_hotkey_string 函数

# TODO: 定义全局实例
